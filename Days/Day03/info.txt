code for Circle 

index.py
simple Circle

 """
    Draws a circle using OpenGL.

    Parameters:
    Circle at the origin
    radius (float): The radius of the circle.
    num_segments (int): The number of segments to divide the circle into. 
              More segments result in a smoother circle.

    The function calculates the angle for each vertex using the formula:
    angle = 2 * π * i / num_segments

    For each vertex, the x and y coordinates are calculated as:
    x = radius * cos(angle)
    y = radius * sin(angle)

    The vertices are then specified using glVertex2f(x, y).
    """

index2.py

to draw a circle not at the origin 


index3.py
4-way symmetrics


index4.py
8-way stmmetrics

